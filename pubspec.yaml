name: flutter_macos_dock
description: A Flutter package that replicates the macOS Dock behavior and appearance with extensive customization options.
version: 0.0.1
homepage: https://github.com/makjac/flutter_macos_dock

environment:
  sdk: ^3.10.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0
# flutter:
